import { suite } from 'mocha';
import { expect } from 'chai';

suite('DevOps Omnibus Extension Test Suite', () => {
    // Add your test cases here
    it('should run a sample test', () => {
        expect(true).to.be.true;
    });
});