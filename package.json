{
  "name": "devops-omnibus-monorepo",
  "private": true,
  "version": "0.0.1",
  "description": "Monorepo containing the DevOps Omnibus VS Code extension and MCP server.",
  "workspaces": [
    "extension",
    "mcp-server",
    "shared"
  ],
  "scripts": {
    "build": "npm run compile -w extension && npm run build -w mcp-server",
    "compile": "npm run compile -w extension",
    "lint": "npm run lint -w extension && npm run lint -w mcp-server",
    "test": "npm test -w extension && npm test -w mcp-server",
    "watch": "npm run watch -w extension",
    "package:extension": "cd extension && npx vsce package"
  }
}

